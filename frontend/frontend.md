# Requirements
Vite
ReactJS
Tanstack Router
GSAP
Tailwind

## Versions
everything should use the latest stable version


### Build

Make a react application which uses vite, tailwindcss and tanstack router, GSAP with crisp animations, it should look like an AI product from 2099, Routing should be done using tanstack router, all api calls should be placed in src/api, all utils should be src/utils, all components should be in src/components, make the folder structure at your discretion, 





